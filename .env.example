# DevTools Hub requires no API keys or secrets.
# All tools run entirely client-side in the browser.
# This file exists as a placeholder for future needs.

# Example (not used):
# NEXT_PUBLIC_API_URL=
